# Ameli Rental — Система учёта склада

> Скажи Claude: «Прочитай CLAUDE.md и помоги продолжить разработку».

---

## Репозиторий и деплой

**GitHub:** https://github.com/serviceameli/ameli-sklad
**GitHub Pages:** https://serviceameli.github.io/ameli-sklad/

**Файлы проекта:**
| Файл | Назначение |
|------|-----------|
| `warehouse-staff.html` | Страница кладовщика (телефон) |
| `warehouse-dashboard.html` | Дашборд руководителя |
| `config.js` | URL Apps Script — менять **только здесь** |
| `google-apps-script.js` | Бэкенд на Google Apps Script |

**Apps Script URL (актуальный):**
```
https://script.google.com/macros/s/AKfycbyPYjJR41JlFRkL14-npAan7uHyYO5fumUwte7pAVV9WzTLoqu1hC5i1Gdr5wRmbhk/exec
```

⚠️ После каждого изменения Apps Script → **новое развёртывание** (New deployment) → новый URL → обновить `config.js`.

**Google Sheets ID:** `10N47vejHTmvFe_ABfmqGDYmtCZWr_VLW5dww5eQ-RQM`

---

## Листы Google Sheets

| Лист | Колонки | Назначение |
|------|---------|-----------|
| `заказы` | A-Z (экспорт сайта) | Список заказов с сайта |
| `log` | 18 колонок | Журнал всех визитов |
| `workers` | A=Имя, B=PIN, C=Ссылка, D=Активен | Кладовщики |
| `drafts` | 4 колонки | Незакрытые смены (JSON) |

### Лист `workers`
- **D=Активен**: пустая или любое значение кроме «нет» → кладовщик активен. «нет» → заблокирован, не попадает в список.
- Имена хранятся ТОЛЬКО в таблице, не в коде.

### Ключевые колонки листа `заказы`
```
0  = Номер заказа
2  = Получение, дата  (дд.мм.гггг)
4  = Возврат, дата
6  = Статус сайта     ← НЕ ИСПОЛЬЗУЕМ для логики склада (только для менеджеров)
16 = Клиент
19 = Работник (если заполнен → «Наша доставка», иначе «Самовывоз»)
```

### Колонки лога (18 колонок)
```
0  Дата смены         7  Операция (Выдача / Возврат)
1  Начало смены UTC   8  Все заказы визита (через запятую)
2  Конец смены UTC    9  Кол-во заказов
3  Кладовщик          10 Первый заказ
4  День/Ночь          14 Время визита МСК
5  Всего визитов      15 Время внесения МСК
6  Кто приехал        17 Комментарий
```

---

## Архитектура логики

### Какие заказы показывать кладовщику — только по датам

| Условие | Отображение |
|---------|------------|
| `issueDate == сегодня` И `returnDate != сегодня` | К выдаче |
| `returnDate == сегодня` И `issueDate != сегодня` | К возврату |
| `issueDate == returnDate == сегодня` | **Однодневная аренда** — сначала К выдаче, после выдачи → К возврату |
| Заказ есть в логе → выдан | Убираем из «К выдаче» |
| Заказ есть в логе → возвращён | Убираем из «К возврату» |
| Просроченные даты + не в логе | 🔴 Просрочен |

> ⚠️ **ВАЖНО**: Колонка `Статус` в листе `заказы` — для менеджеров сайта, склад её НЕ читает.
> Статус определяется ТОЛЬКО по датам + логу визитов.

### Однодневная аренда (`sameDay: true`)
Заказ с `issueDate == returnDate == today` получает флаг `sameDay: true`.
На фронтенде функция `getEffectiveOrderType(o, issuedIds, returnedIds)`:
- не выдан → `'issue'`
- выдан, не возвращён → `'return'`
- возвращён → `'done'`

### Обработанные заказы
`getProcessedOrderIds()` сканирует **весь** лог (без фильтра по дате).
Возвращает `{issued:[...], returned:[...]}` — ID заказов из лога.

`getData` возвращает `processedOrders` → фронтенд кладёт в `serverIssuedToday` / `serverReturnedToday`.
Плюс текущая сессия через `getSessionStatuses()` для мгновенного отклика.

### Операции в логе
Операции: **Выдача** / **Возврат** / **Выдача и возврат** (однодневная).
Старые значения: «Получение (наш)», «Возврат (наш)», «Получение+Возврат» — обратная совместимость сохранена.

---

## Apps Script — эндпоинты

### GET
| action + параметры | Возвращает |
|---|---|
| `getData` | `{workers, orders, processedOrders}` |
| `getData&worker=Имя` | + `draft` (черновик этого кладовщика) |
| `getAll` | `{shifts, orders}` для дашборда |

### POST actions
| `action` в теле | Действие |
|---|---|
| `isDraft: true` | Сохранить черновик в лист `drafts` |
| `action: 'addVisit'` | **Немедленно** записать один визит в лог |
| `action: 'closeShift'` | Удалить строки этой смены + перезаписать все визиты |

---

## Страница кладовщика (`warehouse-staff.html`)

### Персональные ссылки
Каждый кладовщик имеет ссылку: `warehouse-staff.html?worker=Имя+Фамилия`

**Логика при открытии:**
- Нет черновика → **Welcome-экран**: «Открыть смену» / «Посмотреть заказы»
- Черновик есть, < 12 часов → сразу **рабочий экран** (смена восстанавливается)
- Черновик есть, > 12 часов → экран **«Закрыть старую смену»** / «Продолжить»
- Без параметра `?worker=...` → обычный экран выбора кладовщика

### Три вкладки
1. **＋ Визит** — форма добавления визита (кто приехал, какие заказы, время)
2. **📦 Заказы** — статусы всех заказов дня (обновляется каждые 60 сек, видят все кладовщики)
3. **📋 Смена** — журнал визитов смены + кнопка «Закрыть смену»

### Ключевые функции
- `addVisit()` → пишет в лог немедленно через `sendVisitNow()` (action: addVisit)
- `saveDraft()` → localStorage под ключом `wh_draft_ИмяФамилии` (per-worker)
- `getEffectiveOrderType(o, issuedIds, returnedIds)` → тип заказа с учётом sameDay
- `getSessionStatuses()` → объединяет серверные данные + текущая сессия
- `refreshOrdersFromServer()` → каждые 60 сек обновляет заказы

### Важные детали
- `localStorage` ключ per-worker: `wh_draft_Имя_Фамилия` — кладовщики не конфликтуют
- PIN-система закомментирована (отключена), код сохранён для будущего включения
- Даты в `sendVisitNow` и `confirmSubmit` → явный формат `dd.MM.yyyy` через MSK-хелперы
- `deleteShiftRows` сравнивает ISO-даты через `.toISOString()` (Sheets хранит их как Date объекты)

---

## Технические нюансы

1. **`no-cors` режим** — POST-запросы fire-and-forget, ответ не читается
2. **Даты в Sheets** — ISO-строки автоматически конвертируются в Date объекты при записи
3. **Имя листа `заказы`** — строчными, чувствительно к регистру
4. **Даты парсятся** через `parseDate()` — поддерживает `дд.мм.гггг`, `yyyy-mm-dd`, Excel serial
5. **После деплоя Apps Script** — URL меняется, обновить в `config.js` и закоммитить

---

## Что реализовано

### Страница кладовщика (`warehouse-staff.html`)

| Фича | Детали |
|------|--------|
| Персональные ссылки | `?worker=Имя+Фамилия` — прямой вход, без выбора кладовщика |
| Welcome-экран | «Открыть смену» / «Посмотреть заказы» |
| Экран старой смены | Если черновик > 12 часов → «Закрыть ту смену» / «Продолжить» |
| Без личной ссылки | Общая ссылка → сразу режим просмотра (без выбора кладовщика) |
| Три вкладки | ＋ Визит / 📦 Заказы / 📋 Смена |
| Счётчик заказов | Ожидает / Выдано / Возвращено / 🔴 Просрочено — виден всегда |
| Tap-список заказов | Нажатием, не dropdown; секции «К выдаче», «К возврату», «Просрочены» |
| Вертикальные кнопки посетителя | Клиент / Яндекс / Наш курьер |
| Автоопределение операции | Из типа заказа (выдача/возврат), ручной выбор только для «нет в списке» |
| Просроченные заказы | 🔴 в tap-list и счётчике; sameDay-заказы заблокированы до выдачи |
| Однодневная аренда | `sameDay: true` — выдача → возврат → готово |
| Компактный выбор даты | Скрытый picker, по умолчанию сегодня |
| Автосохранение | localStorage каждые 15 сек + на сервер (isDraft), без уведомлений |
| Восстановление смены | localStorage первично, сервер как резерв при входе с другого устройства |
| Режим просмотра | Баннер «Только просмотр», вкладки ＋Визит и Смена скрыты; кнопка «Начать смену →» (только для личной ссылки) |
| Экран успеха | Время смены с–по (shiftStart→shiftEnd), кнопка «Заказы в режиме просмотра» |
| День/Ночь смены | Определяется по времени **начала** смены (`isNightAt(shiftStart)`), а не закрытия |
| Обновление заказов | `refreshOrdersFromServer()` каждые 15 сек |
| `issuedBy` / `returnedBy` | Кто именно выдал/принял — из `processedOrders` |
| Per-worker localStorage | `wh_draft_Имя_Фамилии` — кладовщики не конфликтуют |
| «Заказ не из списка» | Галка при выборе, попадает в секцию «Выдано/Возвращено» со статусом, доставкой и комментарием |
| Комментарии | Видны везде где показываются заказы: в карточках, таблице смены |
| PIN-система | Отключена, код сохранён (закомментирован) |
| Toast-уведомления | Кратковременные сообщения вверху экрана |

### Логика входа (`personalWorkerUrl`)
```
?worker=Имя  → personalWorkerUrl установлен
  → черновик < 12ч → рабочий экран
  → черновик > 12ч → экран «Закрыть старую смену»
  → нет черновика  → welcome-экран

без ?worker= → personalWorkerUrl = null → режим просмотра
  → баннер без кнопки «Начать смену»
  → после закрытия смены resetAll() → welcome своего кладовщика
```

### Дашборд руководителя (`warehouse-dashboard.html`)

| Вкладка | Содержимое |
|---------|-----------|
| 📦 Заказы сегодня | Таблицы «К выдаче» / «К возврату», статусы, доставка, комментарии |
| ⏰ Нагрузка | Тепловая карта час × день недели, почасовые графики, автовыводы (пик/тихий час) |
| 📊 Статистика | Визиты/заказы за период, timeline, по дням недели, по месяцам, pie-chart |
| 🧑 Кладовщики | Карточки: визиты, заказы, коэф., день/ночь, по типу посетителя |
| 🕐 Табель часов | Расчёт дневных/ночных часов, экспорт CSV |
| 🔗 Сверка | Визиты «нет в списке», привязка к заказам, бейдж-уведомление |
| 📋 Журнал | Таблица всех визитов с фильтром по периоду, экспорт CSV |

### Apps Script (`google-apps-script.js`)

| Функция / возможность | Детали |
|-----------------------|--------|
| `getData` | workers + orders (сегодня + просрочка) + processedOrders (с issuedBy/returnedBy) + draft |
| `getAll` | shifts + orders для дашборда |
| `addVisit` | Немедленная запись визита в лог |
| `closeShift` | Удаление строк смены + перезапись всех визитов |
| `isDraft` | Сохранение черновика в лист `drafts` |
| `getWorkerDraft` | Черновик конкретного кладовщика — резерв при входе с другого устройства |
| `getProcessedOrderIds` | Весь лог → {issued, returned, issuedBy, returnedBy} |
| `getTodayOrders` | Заказы на сегодня по датам (не по статусу) |
| `getOverdueOrders` | Просрочка: из листа заказов + из лога (Просрочен-записи + Выдача без Возврата) |
| `recordOverdueOrders` | Автозапись просрочки в лог, триггер 00:00 МСК ежедневно |
| `setupOverdueTrigger` | Создаёт/пересоздаёт триггер (запустить один раз вручную) |
| `getAllShifts` | Агрегация лога → смены с вложенными визитами |
| `updateOrderStatuses` | Ставит «Выполнен» в лист заказов (колонка G) |
| `saveDraft` / `clearDraft` | Управление черновиками в листе `drafts` |
| `setupSheets` / `checkTodayOrders` | Утилиты первичной настройки |
| Обратная совместимость | Старые операции («Получение (наш)», «Получение+Возврат» и др.) читаются корректно |

---

## Инструкции для Claude

После каждого изменения файлов:
```
git add . && git commit -m "описание" && git push
```

**Не трогать**: колонку `Статус` (G) в листе `заказы` для логики склада — только для менеджеров.

---

## Сессия 2 июня 2026 — что сделано

### Apps Script (требует нового деплоя после каждого изменения)
- `getProcessedOrderIds` — читает 18 колонок (было 9), возвращает `otherVisits` (визиты «не из списка» из лога)
- `deleteVisit` — новый action: удаляет строку лога по shiftStart + worker + timeAuto
- `deleteVisitRow()` — вспомогательная функция удаления строки
- `time` в `otherVisits` форматируется через `Utilities.formatDate` (не `.toString()`)
- **Актуальный URL:** `https://script.google.com/macros/s/AKfycbxdyP66s9-fsfmgL4MrzQrZWnj4wRUtq6PzFVLSPnSTXoeWfuLOQ0wG4-OEApEwUTk/exec`

### warehouse-staff.html — что изменилось
| Фича | Детали |
|------|--------|
| Кнопки «Кто приехал» | Горизонтальный ряд (3 в строку), иконка + текст |
| Время + дата | В одну строку: `[17:30] 📅 Вт, 2 июня ✏️` |
| Комментарий | Отдельной строкой под временем, `<input type="text">` (было textarea) |
| Список заказов | Перенесён ПОСЛЕ кнопки «+ Добавить визит» |
| Заголовок заказов | «Заказы в визите (нажмите чтобы добавить)» |
| `serverOtherVisits` | Новая переменная, заполняется из `processedOrders.otherVisits` |
| `getOtherVisits()` | Новая функция: объединяет session + server «не из списка» без дублей по time |
| Счётчики плашки | «не из списка» теперь учитываются в «выдано» / «возвращено» |
| Мини-статистика на Заказы | «не из списка» учитываются в ic/rc |
| Режим просмотра | `serverOtherVisits` подгружается с сервера → «не из списка» видны после закрытия смены |
| Удаление визита | Диалог подтверждения с именами заказов; откат `serverIssuedToday`/`serverReturnedToday`/`serverOtherVisits`; отправка `deleteVisit` на сервер |
| Успех-экран | Вместо «День/Ночь» — `calcShiftHours()`: «7ч (☀️ 3ч дневных · 🌙 4ч ночных)» |
| `calcShiftHours(start, end)` | Новая функция: считает дневные/ночные часы посекундно (ночь 22:00–09:00 МСК) |

---

## Статус тестирования (сессия 2 июня)

### Проверено ✅
1. Загрузка страницы, восстановление черновика
2. Горизонтальные кнопки «Кто приехал»
3. Дата рядом со временем в одну строку
4. Список заказов под кнопкой «Добавить визит»
5. Добавление визита с реальным заказом → тост, счётчики, заказ исчезает из «К выдаче»
6. Добавление «Заказ не из списка» → тег, блок операции, счётчики +1
7. Вкладка Заказы → мини-статистика совпадает с плашкой
8. Секция «Выдано/Возвращено» → «не из списка» из сессии + сервера видны с кладовщиком
9. Вкладка Смена → «⚠️ Заказ не из списка» помечен тегом

### Не проверено (браузер зависал на confirm() диалоге)
- Диалог подтверждения удаления визита — текст сообщения
- Откат счётчиков после удаления
- Возврат заказа в «К выдаче» после удаления
- Закрытие смены → экран с дневными/ночными часами
- Режим просмотра после закрытия смены

### Известная проблема с тестированием
`confirm()` диалог блокирует CDP (Chrome DevTools Protocol) → браузер зависает при авто-тестировании.
**Решение для следующей сессии**: перед кликом × выполнить JS:
```js
window.confirm = function(msg){ window._confirmMsg = msg; return true; }
```
Затем кликнуть ×, затем проверить `window._confirmMsg`.

### Тестовые данные в логе
В ходе тестирования добавлены 2 визита в смену Алевтины Разореновой:
- 17:30 — Клиент, Выдача, 26-А-000943
- 17:31 — Клиент, Выдача, «Заказ не из списка»
Если смена не была закрыта — эти строки остались в логе. При закрытии смены запишутся корректно.
